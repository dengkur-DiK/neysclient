// This file is intentionally minimal: react-app-rewired expects
// a module named `config-overrides.js`. The repository contains
// `configoverides.js` (note the missing dash). To avoid renaming the
// existing file we re-export it here.

module.exports = require('./configoverides');
